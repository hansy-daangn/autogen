# GO-92 — 리스너 원격화 (터널 + 폰 원격 실행)

## 임무 (결론부터)

`go_listener.py`를 cloudflared 터널로 https 노출하고, 터널 URL을 사이트 저장소
(`hansy-daangn/autogen`, branch `claude/new-session-aqqyl5`)의 `listener.json`으로 push한다.
이것으로 폰 대시보드(https://hansy-daangn.github.io/autogen/)에서 ▶ 실행·AUTO·상태·로그가
**즉시, 직접** 동작한다. 대시보드 쪽 연동은 이미 배포 완료 — 이 GO는 로컬 리스너 쪽만 고친다.

## 특례

인프라 태스크 — 라운드 로그·첨부·seed 의무 면제. result.json + log.csv 1행(stage:tunnel)만.

## 선행 조건 게이트 — 미충족 시 BLOCKED

- `cloudflared --version` 실패 → `winget install Cloudflare.cloudflared` 후 재실행.
- `autogen` 저장소에 push 가능한 git 인증 (GO-90과 동일 게이트).

## 실행 절차

1. **토큰 로테이션 (필수, 1회)** — 기존 `kj_7f3a9c2e51d84b60a1`은 공개 저장소에 노출되었다.
   새 토큰을 생성해 `go_listener.py`의 검증값으로 교체한다. **새 토큰은 어떤 파일로도
   저장소에 올리지 않는다.** 폰에서는 대시보드 주소 뒤에 `?token=<새토큰>` 을 붙여 1회
   접속하면 기기에 저장된다 (이후에는 그냥 접속).
2. **CORS** — `go_listener.py`의 모든 HTTP 응답에 헤더 추가:
   `Access-Control-Allow-Origin: *` ·
   `Access-Control-Allow-Headers: Content-Type, X-Kanje-Token` ·
   `Access-Control-Allow-Methods: GET, POST, OPTIONS`.
   `OPTIONS` 요청은 무조건 204로 응답 (preflight).
3. **터널 기동** — 리스너 시작 시 서브프로세스로
   `cloudflared tunnel --url http://127.0.0.1:8787 --no-autoupdate` 를 띄우고,
   출력에서 `https://…trycloudflare.com` URL을 파싱한다 (60초 내 미검출 시 경고 후 로컬 전용으로 계속).
4. **URL 발행** — `listener.json` 을 `{"url":"https://xxx.trycloudflare.com","updated":"<ISO8601>"}`
   내용으로 autogen 클론(작업 폴더: `C:\Users\USER\Desktop\Workspace\관제_사이트_202608\`)
   루트에 쓰고 commit + push. 대시보드는 raw.githubusercontent에서 이를 읽어 자동 연결한다
   (Pages 재배포를 기다릴 필요 없음).
5. **검증** — 폰 브라우저(또는 PC curl)에서 `터널URL/state` 가 200인지, 대시보드 접속 시
   리스너가 "온라인"으로 뜨고 ▶ 실행이 접수되는지 확인.

## 보안 수칙

- 터널 URL과 저장소는 **공개**다. `/go`·`/auto` 는 `X-Kanje-Token` 불일치 시 반드시 403.
- 토큰을 코드·저장소·대시보드 HTML에 하드코딩하지 않는다 (대시보드는 localStorage 사용).
- quick tunnel URL은 리스너 재시작마다 바뀐다 — 매 시작 시 4번(listener.json push)이 갱신 채널이다.
  고정 URL이 필요해지면 tailscale funnel 대안을 별도 GO로.

## 산출

```json
{"id":"GO-92","verdict":"DONE","summary":"터널: https://xxx.trycloudflare.com — 폰 원격 실행 활성",
 "next":"리스너 재시작마다 터널 URL 자동 재발행 · 폰은 ?token= 1회 등록"}
```
